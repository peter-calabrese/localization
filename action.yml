name: Translate locale files
description: Translate English locale files

inputs:
  files:
    description: Locale JSON files to translate
    required: true
  locales-root:
    description: Root directory containing language folders
    required: false
    default: src/locales

runs:
  using: node24
  main: dist/index.mjs
